# ARVELIS AI — Smoke Test Preview QA Candidate

Этот сценарий предназначен для контрольного запуска после будущего merge в `main` и Pull в Replit.

## Перед тестом

- Replit подключён к `ilsafgilmullin/arvelis-ai`;
- активная ветка — `main`;
- локальных незакоммиченных изменений нет;
- Replit Agent не требуется.

## 1. Запуск

1. Выполнить Pull в Git-разделе Replit.
2. Нажать Run.
3. Дождаться установки npm-зависимостей и старта Vite.
4. Открыть Web Preview.

Ожидаемый результат: открывается welcome screen ARVELIS AI без горизонтального переполнения и ошибок загрузки.

## 2. Welcome

Проверить:

- утверждённый золотой знак ARVELIS;
- надпись ARVELIS / AI / `INTELLIGENCE. PRECISION. RESULTS.`;
- графитово-золотой премиальный стиль без визуального шума;
- маркировку `PRODUCT PREVIEW`;
- заголовок `Добро пожаловать в ARVELIS AI.`;
- основную кнопку `Открыть ARVELIS AI`;
- вторичную кнопку `Продолжить с именем`;
- на узком телефоне обе CTA-кнопки переходят в одну колонку и не выходят за экран.

## 3. Smart Entry — реальная загрузка

### Прямой вход

1. На Welcome нажать `Открыть ARVELIS AI`.
2. Должен появиться фирменный loading-screen с полным логотипом ARVELIS AI.
3. Прогресс должен изменяться только по мере завершения реальных задач подготовки.
4. Не должно быть искусственной паузы фиксированной длительности.
5. После готовности критического ядра автоматически открывается ARVELIS AI.

Реальные критические задачи текущего preview:

- загрузка App Layout + стартового экрана;
- загрузка Chat-модуля;
- восстановление/проверка локального preview-state.

Ожидаемый результат: на холодном старте loading-screen виден дольше; при уже прогретом browser cache переход может быть очень быстрым. Это корректно и **не считается ошибкой**.

### Lazy fallback

Если отдельный раздел реально ещё не прогрелся, допускается краткий indeterminate loading-screen `Открываем раздел` **без выдуманного процента**.

### Ошибка загрузки

При отказе критического dynamic import должен появиться понятный retry-state, а не белый экран.

## 4. Preview Access / персонализация

1. На Welcome нажать `Продолжить с именем`.
2. Убедиться, что форма **не просит email, пароль, токен или другой секрет**.
3. Ввести отображаемое имя.
4. Нажать `Продолжить в ARVELIS AI`.

Ожидаемый результат:

- появляется реальный Smart Entry loading-screen;
- loading-screen приветствует пользователя по введённому имени;
- после загрузки ARVELIS AI также приветствует пользователя по имени;
- реальная учётная запись не создаётся;
- используется только локальное отображаемое имя.

## 5. Стартовый экран ARVELIS AI

Проверить:

- Topbar называется `ARVELIS AI`, а не `Рабочее пространство`;
- subtitle показывает дружелюбное приветствие по времени суток;
- при пользовательском имени приветствие содержит имя;
- основной вопрос — `С чего начнём?`;
- composer принимает текст;
- счётчик символов меняется;
- пустой запрос отправить нельзя;
- быстрые сценарии `Рабочая задача`, `Учебный разбор`, `Сравнение вариантов` открывают локальный диалог;
- длинный текст и длинная строка без пробелов не создают horizontal overflow;
- счётчик истории использует корректную форму `диалог / диалога / диалогов`.

Дополнительно: прокрутить стартовый экран вниз, перейти в другой раздел и вернуться. Экран должен открыться с начала страницы, а не с прежней позиции прокрутки.

## 6. Chat

1. Создать новый диалог.
2. Убедиться, что кнопка `+` внутри composer отключена, пока новый пустой диалог уже открыт.
3. Ввести короткое сообщение и отправить.
4. Ввести многострочное/длинное сообщение.
5. Вставить длинный URL или строку без пробелов.
6. Убедиться, что textarea растёт по содержимому до разумного предела.
7. Отправить несколько сообщений подряд.
8. После появления существующего диалога проверить, что кнопка `+` создаёт новый диалог.
9. Переключиться на другой раздел и вернуться.

Ожидаемый результат:

- сообщения остаются в текущем preview-сеансе;
- при доступном localStorage они переживают обычную перезагрузку;
- после добавления сообщения интерфейс прокручивается к последнему сообщению;
- интерфейс честно сообщает, что запрос к модели не выполняется;
- MOCK-текст явно маркирован как предзаписанный;
- длинный URL/слово переносится и не расширяет страницу.

На desktop: Enter отправляет, Shift+Enter переносит строку.

На телефоне: при фокусе в input/textarea нижняя навигация должна временно скрываться, а chat composer должен находиться рядом с доступной областью над клавиатурой без большого пустого зазора.

## 7. History

Проверить:

- новый диалог появился в истории;
- строки показывают количество сообщений и относительное время обновления;
- поиск находит диалог по заголовку/сообщению;
- если история пуста без поискового запроса, показано `История пока пуста`;
- если запрос не дал результатов, показано отдельное состояние `Совпадений нет`;
- открытие строки возвращает в нужный диалог;
- при нажатии удаления сначала появляется окно подтверждения;
- `Отмена` сохраняет диалог;
- подтверждение удаляет только выбранный локальный диалог;
- длинные названия не ломают ширину строки.

Для confirm-dialog дополнительно проверить на desktop:

- `Escape` закрывает окно;
- `Tab` и `Shift+Tab` не выводят фокус за пределы окна;
- после закрытия фокус возвращается на кнопку, открывшую подтверждение.

## 8. Profile

Проверить:

- если имя не изменено, кнопка сохранения отключена;
- после изменения имени кнопка становится активной;
- после сохранения отображается состояние `Сохранено`;
- при доступном localStorage после перезагрузки имя остаётся;
- длинное имя до допустимого лимита не ломает layout;
- `Сбросить preview-данные` сначала показывает подтверждение;
- `Отмена` не меняет данные;
- подтверждение сброса восстанавливает стартовое состояние;
- `Состояния интерфейса` остаются доступны из Profile, но не засоряют основную desktop-навигацию;
- touch Safari не оставляет залипший золотой hover после обычного касания строк настроек.

## 9. Offline

После загрузки preview временно отключить сеть на устройстве.

Ожидаемый результат: интерфейс показывает offline banner; уже загруженная локальная часть приложения остаётся доступна и не выдаёт offline-режим за работу AI.

## 10. Local storage failure

Обычному пользователю не требуется специально ломать хранилище. Критерий реализации:

- если localStorage недоступен, переполнен или preview-состояние превышает безопасный локальный бюджет, приложение продолжает работать в текущей сессии;
- сверху появляется предупреждение, что изменения могут исчезнуть после перезагрузки;
- приложение не должно молча удалять старые диалоги для освобождения места;
- последняя корректно сохранённая версия в localStorage не должна перезаписываться заведомо некорректным/слишком большим состоянием;
- интерфейс не утверждает, что данные гарантированно сохранены;
- известные старые служебные demo-тексты могут безопасно нормализоваться в preview-терминологию без изменения пользовательских сообщений.

## 11. System States

Через Profile открыть `Состояния интерфейса` и проверить вкладки:

- Загрузка;
- Пусто;
- Ошибка;
- Офлайн;
- Лимит.

Все состояния должны быть явно обозначены как `PREVIEW`. На внутреннем QA-экране не должно быть кнопок, которые выглядят рабочими, но ничего не делают.

## 12. Runtime fallback

Обычный пользователь не должен специально ломать приложение для этой проверки. Критерий реализации: если React-дерево получает необработанную runtime-ошибку, вместо пустого белого экрана должен появиться брендированный экран `INTERFACE ERROR` с кнопкой перезагрузки.

## 13. iPhone portrait

Проверить:

- нет горизонтальной прокрутки;
- верхняя часть не залезает под Dynamic Island/notch;
- нижняя навигация учитывает safe area;
- touch targets удобно нажимаются;
- клавиатура не закрывает поле ввода;
- mobile navigation скрывается во время ввода и возвращается после закрытия клавиатуры;
- focused chat composer не оставляет большой пустой зазор над клавиатурой;
- фокус на input/textarea не вызывает нежелательный zoom Safari;
- длинный текст не ломает ширину;
- `PREVIEW` chip не конфликтует с заголовком Topbar.

## 14. iPhone landscape

Повернуть устройство горизонтально и проверить стартовый экран, Chat, History и Profile.

Ожидаемый результат:

- телефон не переключается в desktop-sidebar только из-за большей CSS-ширины;
- используется компактная нижняя навигация;
- учитываются левый/правый safe-area inset;
- composer и клавиатура не перекрываются навигацией;
- нет горизонтального overflow.

## 15. Узкий экран

На ширине около 320–360 px проверить Welcome, Smart Entry, стартовый экран, History и Profile.

Ожидаемый результат: логотип, заголовки, `PREVIEW` chip, кнопки, progress и нижняя навигация не выходят за границы экрана и остаются читаемыми.

## 16. Desktop / tablet

На desktop проверить:

- sidebar остаётся фиксированной рабочей навигацией;
- в основной sidebar нет внутреннего пункта `Состояния интерфейса`;
- активный пункт навигации визуально выделен и имеет корректный `aria-current`;
- контент не растягивается чрезмерно;
- стартовый экран имеет двухзонную композицию на широком экране;
- на промежуточной ширине примерно 781–1180 px экран безопасно перестраивается в одну колонку и не создаёт overflow;
- confirm-dialog доступен с клавиатуры;
- нижняя mobile navigation не отображается на обычном desktop.

## Что считать ошибкой

- белый/пустой экран;
- npm/Vite error в Run;
- бесконечная загрузка;
- искусственная задержка загрузочного экрана, не связанная с реальной готовностью;
- отображение фиктивного процента для indeterminate lazy loading;
- горизонтальный overflow;
- новый раздел открывается в случайной старой позиции прокрутки;
- кнопка визуально нажимается, но не выполняет описанное локальное действие;
- mock/preview отображается как настоящий AI/backend;
- preview-вход просит реальные учётные секреты;
- интерфейс утверждает, что данные сохранены, когда localStorage недоступен;
- данные молча удаляются из-за внутреннего preview-лимита;
- удаление или полный сброс происходят без подтверждения;
- confirm-dialog позволяет фокусу уйти за окно;
- нижняя навигация перекрывает composer/клавиатуру;
- landscape на телефоне неожиданно показывает desktop-sidebar.

## Отложенная инфраструктурная задача

`package-lock.json` пока отсутствует. Не создавать его вручную. Он должен быть получен только реальной установкой npm-зависимостей в доверенном окружении и затем проверен перед коммитом.

При обнаружении ошибки достаточно прислать скриншот экрана и, если доступен, скрин Console — Shell не требуется.
