run = "npm ci --include=dev --no-audit --no-fund && npm run dev:auth:closed-test"
entrypoint = "index.html"
modules = ["nodejs-22"]

[nix]
channel = "stable-24_11"

[[ports]]
localPort = 3000
externalPort = 80
