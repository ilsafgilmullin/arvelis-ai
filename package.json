{
  "name": "arvelis-ai",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0 <27"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "start": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "typecheck": "tsc -b --pretty false",
    "test:auth": "node scripts/prepare-smoke-runtime.mjs auth && tsc -p tsconfig.auth-smoke.json && node tests/.dist-auth/tests/auth-core-smoke.js && node tests/.dist-auth/tests/preview-profile-smoke.js && node tests/.dist-auth/tests/demo-storage-profile-smoke.js && node tests/.dist-auth/tests/protocol-text-smoke.js && node tests/.dist-auth/tests/protocol-shape-smoke.js",
    "test:server-auth": "node scripts/prepare-smoke-runtime.mjs server-auth && tsc -p tsconfig.server-auth-smoke.json && node tests/.dist-server-auth/tests/email-otp-core-smoke.js && node tests/.dist-server-auth/tests/session-core-smoke.js",
    "check": "npm run typecheck && npm run test:auth && npm run test:server-auth && npm run build"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.0.5",
    "typescript": "6.0.3",
    "vite": "8.2.1"
  }
}
